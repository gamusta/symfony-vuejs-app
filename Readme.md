

### Prerequisites

Docker

### Installation and startup

* Build docker images and run containers
  ```sh
  make build
  ```
* Init database
  ```sh
  make db-init
  ```

* Fetch fruits data
  ```sh
  make fruits-fetch
  ```